{
  "name": "swampys-website",
  "version": "1.0.0",
  "description": "Swampy's personal website - the cutest crocodile in the world",
  "scripts": {
    "install-all": "npm install && cd client && npm install",
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
